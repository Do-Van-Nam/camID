class PackageItem {
  final String name;
  final String data;
  final String price;
  final String expired;
  final String description;

  PackageItem(this.name, this.data, this.price, this.expired, this.description);
}